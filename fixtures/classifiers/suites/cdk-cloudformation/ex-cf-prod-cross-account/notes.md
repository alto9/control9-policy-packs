# ex-cf-prod-cross-account

CloudFormation change set adding a cross-account principal to an S3 bucket policy.

**Tool family:** `cloudformation`

**Resource identity:** `SharedDataBucketPolicy`

**Classifier labels:** `cross-account-access`
