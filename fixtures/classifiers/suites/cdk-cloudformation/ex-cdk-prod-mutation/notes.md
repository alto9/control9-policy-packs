# ex-cdk-prod-mutation

CDK synthesized template updating production S3 bucket versioning.

**Tool family:** `cdk`

**Resource identity:** `ProdAppBucket`

**Classifier labels:** `resource-update`

**Matched rule:** `require-approval-prod-mutation` -> `require_approval` (medium)

Production target detection: environment is production and the change mutates live infrastructure.
