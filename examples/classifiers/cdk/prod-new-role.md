# CDK synthesized template new role example

CDK synth output is represented as a synthesized CloudFormation template JSON artifact.

**Input artifact:** `packs/production-infra-baseline/fixtures/inputs/cdk/prod-iam-role-template.json`

**Stack identity:** `ProdNetworkStack`

**Logical resource identity:** `BatchWorkerRole` (`AWS::IAM::Role`)

**Classifier labels:** `new-role`

**Matched rule:** `require-approval-prod-new-role` → `require_approval` (high)

See case `cf-cdk-prod-new-role` in `classifier-cases.json`.
